import { Settings } from "@/components/admin/pages/Settings";

export const metadata = {
  title: "Settings | FitTrack Admin",
  description: "Application settings",
};

export default function SettingsPage() {
  return <Settings />;
}
