import { Account } from "@/components/admin/pages/Account";

export const metadata = {
  title: "Account Security | FitTrack Admin",
  description: "Manage your account security settings",
};

export default function AccountPage() {
  return <Account />;
}
