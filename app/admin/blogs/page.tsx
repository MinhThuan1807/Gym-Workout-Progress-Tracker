import { Blogs } from "@/components/admin/pages/Blogs";

export const metadata = {
  title: "Blog Management | FitTrack Admin",
  description: "Manage blog posts",
};

export default function BlogsPage() {
  return <Blogs />;
}
